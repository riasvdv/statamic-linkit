{
  "private": true,
  "scripts": {
    "dev": "NODE_OPTIONS=--openssl-legacy-provider npx mix",
    "development": "NODE_OPTIONS=--openssl-legacy-provider npx mix",
    "watch": "NODE_OPTIONS=--openssl-legacy-provider npx mix watch",
    "prod": "NODE_OPTIONS=--openssl-legacy-provider npx mix --production",
    "production": "NODE_OPTIONS=--openssl-legacy-provider npx mix --production",
    "format": "prettier 'resources/**/*.{css,js,json,css,ts,tsx,vue}' '*.{js,css}' --write"
  },
  "devDependencies": {
    "laravel-mix": "^6.0",
    "postcss": "^8.1",
    "prettier": "^1.18.2",
    "vue": "^2.6.12",
    "vue-loader": "^15.9.5",
    "vue-template-compiler": "^2.6.12"
  }
}
